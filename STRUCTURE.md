# 📂 Структура проекта - Краткая справка

## Быстрая навигация

```
src/
│
├── features/           # ⭐ Фичи приложения
│   ├── auth/          # Авторизация
│   └── dashboard/     # Главная страница
│
├── shared/            # 🔄 Переиспользуемый код
│   ├── composables/
│   ├── lib/
│   ├── types/
│   └── ui/
│
├── widgets/           # 🧩 Сложные блоки UI
│
└── app/               # ⚙️ Конфигурация приложения
    └── layouts/       # Лейауты
```

## Шаблон фичи

```
features/
└── [feature-name]/
    ├── api/          # API запросы
    ├── config/       # Константы
    ├── model/        # Типы, composables
    ├── store/        # Pinia store
    └── ui/           # Компоненты
```

## Где что находится

| Что искать | Где найти |
|-----------|-----------|
| Страницы | `features/*/ui/*Page.vue` |
| Компоненты фичи | `features/*/ui/` |
| Общие компоненты | `shared/ui/` |
| API запросы | `features/*/api/` |
| Stores | `features/*/store/` |
| Типы | `features/*/model/types.ts` или `shared/types/` |
| Роуты | `app/router/index.ts` |
| Лейауты | `app/layouts/` |

## Быстрый старт новой фичи

```bash
# 1. Создать структуру
mkdir -p src/features/[name]/{api,config,model,store,ui}

# 2. Создать типы
touch src/features/[name]/model/types.ts

# 3. Создать UI
touch src/features/[name]/ui/[Name]Page.vue

# 4. Добавить роут в app/router/index.ts
```

Подробная документация: [ARCHITECTURE.md](./ARCHITECTURE.md)




