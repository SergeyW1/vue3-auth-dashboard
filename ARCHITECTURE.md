# 🏗️ Архитектура проекта

## Feature-Based Architecture

Проект использует **Feature-Based Architecture** - современный подход к организации кода, который группирует файлы по бизнес-фичам, а не по типам файлов.

## 📁 Структура проекта

```
src/
├── features/              # Фичи приложения (основная единица организации)
│   ├── auth/              # Фича авторизации
│   │   ├── api/           # API логика фичи
│   │   │   └── authApi.ts
│   │   ├── config/        # Конфигурация и константы
│   │   │   └── constants.ts
│   │   ├── model/         # Типы и бизнес-логика
│   │   │   └── types.ts
│   │   ├── store/         # Store фичи (Pinia)
│   │   │   └── authStore.ts
│   │   └── ui/            # UI компоненты фичи
│   │       └── LoginPage.vue
│   │
│   └── dashboard/         # Фича дашборда
│       └── ui/
│           └── HomePage.vue
│
├── shared/                # Переиспользуемый код между фичами
│   ├── composables/       # Общие composables
│   ├── lib/               # Утилиты и хелперы
│   │   ├── api/           # API утилиты (базовые функции для запросов)
│   │   └── utils/         # Общие утилиты
│   ├── types/             # Общие типы TypeScript
│   └── ui/                # Переиспользуемые UI компоненты
│       ├── Button/
│       ├── Input/
│       └── Card/
│
├── widgets/               # Сложные переиспользуемые блоки (опционально)
│   └── Header/
│       ├── Header.vue
│       └── UserMenu.vue
│
├── app/                   # Инициализация приложения
│   ├── layouts/           # Лейауты приложения
│   │   └── AppLayout.vue
│   ├── router/            # Конфигурация роутинга
│   │   └── index.ts
│   └── store/             # Глобальные stores (если нужны)
│
├── plugins/               # Плагины Vue
│   └── vuetify.ts
│
├── assets/                # Статические ресурсы
├── App.vue                # Корневой компонент
└── main.ts                # Точка входа
```

## 🎯 Принципы архитектуры

### 1. Фича (Feature) - основная единица организации

**Фича** - это изолированный модуль, содержащий всю логику для конкретной бизнес-функциональности:

- ✅ **Все файлы фичи в одной папке** - легко найти и понять
- ✅ **Независимость** - фичу можно легко переместить или удалить
- ✅ **Изоляция** - минимизация зависимостей между фичами

**Структура фичи:**

```
features/
└── auth/                  # Название фичи
    ├── api/               # Взаимодействие с API
    ├── config/            # Конфигурация, константы
    ├── model/             # Типы, интерфейсы, бизнес-логика
    ├── store/             # State management (Pinia)
    └── ui/                # UI компоненты (страницы, формы)
```

### 2. Shared - переиспользуемый код

**Shared** содержит код, который используется несколькими фичами:

- ✅ Общие UI компоненты (кнопки, инпуты)
- ✅ Утилиты (форматирование дат, валидация)
- ✅ API утилиты (базовые функции для HTTP запросов)
- ✅ Общие типы

**Правило:** Если код используется только в одной фиче - он должен быть в фиче, а не в shared.

### 3. App - инфраструктура приложения

**App** содержит код для инициализации и настройки приложения:

- ✅ Роутинг
- ✅ Лейауты
- ✅ Глобальные настройки

### 4. Pages - тонкий слой маршрутизации (опционально)

В текущей реализации страницы импортируются напрямую из `features` в роутере. Это допустимо, но можно создать тонкий слой `pages/`, который будет реэкспортировать страницы из фич:

```typescript
// pages/LoginPage.vue
export { default } from '@/features/auth/ui/LoginPage.vue';
```

## 📝 Шаблон создания новой фичи

При создании новой фичи следуйте этой структуре:

### Пример: Создание фичи "Profile"

```bash
src/features/profile/
├── api/
│   └── profileApi.ts      # API запросы для профиля
├── config/
│   └── constants.ts       # Константы (если нужны)
├── model/
│   ├── types.ts           # TypeScript типы
│   └── useProfile.ts      # Composable с бизнес-логикой
├── store/
│   └── profileStore.ts    # Store (если нужен)
└── ui/
    ├── ProfilePage.vue    # Страница профиля
    ├── ProfileForm.vue    # Форма редактирования
    └── ProfileCard.vue    # Карточка профиля
```

### Шаг 1: Создать структуру папок

```bash
mkdir -p src/features/profile/{api,config,model,store,ui}
```

### Шаг 2: Создать типы

```typescript
// features/profile/model/types.ts
export interface Profile {
  id: string;
  name: string;
  email: string;
  avatar?: string;
}
```

### Шаг 3: Создать API

```typescript
// features/profile/api/profileApi.ts
import type { Profile } from '../model/types';

export const fetchProfile = async (id: string): Promise<Profile> => {
  // API логика
};
```

### Шаг 4: Создать UI компонент

```vue
<!-- features/profile/ui/ProfilePage.vue -->
<script setup lang="ts">
import { fetchProfile } from '../api/profileApi';
// ...
</script>

<template>
  <!-- UI -->
</template>
```

### Шаг 5: Добавить роут

```typescript
// app/router/index.ts
{
  path: '/profile',
  name: 'Profile',
  component: () => import('@/features/profile/ui/ProfilePage.vue'),
  meta: {
    requiresAuth: true,
  },
}
```

## 🔗 Правила импорта

### ✅ Правильно

```typescript
// Внутри фичи можно импортировать напрямую
import { authenticate } from '../api/authApi';
import { useAuthStore } from '../store/authStore';

// Из другой фичи через публичный API (если есть)
import { useAuthStore } from '@/features/auth/store/authStore';

// Shared код можно импортировать везде
import { formatDate } from '@/shared/lib/utils';
```

### ❌ Неправильно

```typescript
// ❌ Не импортируйте внутренние компоненты фичи извне
import LoginForm from '@/features/auth/ui/LoginForm.vue'; // Только если это публичный API

// ❌ Не создавайте циклические зависимости между фичами
// Фича A → Фича B → Фича A
```

## 🎨 Организация UI компонентов

### Компоненты фичи (features/\*/ui/)

Компоненты, специфичные для конкретной фичи:

- Используются только внутри фичи
- Могут быть импортированы другими фичами только через публичный API
- Пример: `LoginForm`, `ProfileCard`

### Shared UI компоненты (shared/ui/)

Переиспользуемые UI компоненты:

- Используются в нескольких фичах
- Не содержат бизнес-логики
- Пример: `Button`, `Input`, `Card`, `Modal`

### Widgets

Сложные переиспользуемые блоки:

- Состоят из нескольких компонентов
- Могут содержать бизнес-логику
- Пример: `Header`, `Sidebar`, `DataTable`

## 📦 Store (Pinia)

### Store внутри фичи

```typescript
// features/auth/store/authStore.ts
export const useAuthStore = defineStore('auth', {
  // Store только для фичи auth
});
```

### Глобальный store (редко используется)

```typescript
// app/store/globalStore.ts
export const useGlobalStore = defineStore('global', {
  // Store для глобального состояния
});
```

**Рекомендация:** Старайтесь держать stores внутри фич для лучшей инкапсуляции.

## 🔄 Миграция существующего кода

При добавлении нового функционала:

1. **Определите границы фичи** - что входит в новую функциональность?
2. **Создайте структуру фичи** - используйте шаблон выше
3. **Перенесите код** - компоненты, логика, API
4. **Обновите импорты** - проверьте все зависимости
5. **Проверьте работоспособность** - запустите тесты и линтер

## ✅ Преимущества Feature-Based Architecture

1. **Масштабируемость** - легко добавлять новые фичи
2. **Поддерживаемость** - все файлы фичи в одном месте
3. **Переиспользуемость** - shared код четко определен
4. **Командная работа** - разные разработчики могут работать над разными фичами
5. **Тестируемость** - фичи легко тестировать изолированно

## 📚 Дополнительные ресурсы

- [Feature-Sliced Design](https://feature-sliced.design/) - более строгая версия этой архитектуры
- [Vue 3 Composition API](https://vuejs.org/guide/extras/composition-api-faq.html)
- [Pinia Documentation](https://pinia.vuejs.org/)

## 🚀 Следующие шаги

1. Создавайте новые фичи по шаблону
2. Выносите общий код в `shared/`
3. Держите фичи изолированными
4. Документируйте публичные API фич (если нужно)

---

**Версия документа:** 1.0  
**Дата обновления:** 2024
