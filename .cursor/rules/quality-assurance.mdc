---
alwaysApply: true
---

# 🔍 ПРАВИЛА КОНТРОЛЯ КАЧЕСТВА И ПРОВЕРКИ ОШИБОК

**⚠️ КРИТИЧЕСКИ ВАЖНО:** После каждого изменения кода ОБЯЗАТЕЛЬНО выполнять проверки.

## Обязательные проверки после изменений:

### 1. **Проверка линтера**

- ✅ ВСЕГДА вызывать `read_lints` для всех измененных файлов
- ✅ Исправлять ВСЕ найденные ошибки перед завершением задачи
- ✅ Проверять не только измененные файлы, но и связанные с ними

### 2. **Проверка типизации TypeScript**

- ✅ Проверять типы импортов (особенно для Vue компонентов, как `@/components/Home.vue`)
- ✅ Убеждаться, что все типы корректно определены
- ✅ Проверять наличие файлов декларации типов (например, `env.d.ts` для Vue компонентов)
- ✅ Использовать `vue-tsc` для проверки типов, если доступно

### 3. **Тестирование**

- ✅ Запускать проект в dev режиме для проверки работоспособности (если возможно)
- ✅ Проверять, что изменения не ломают существующий функционал
- ✅ Убеждаться, что все импорты разрешаются корректно

### 4. **Проверка зависимостей**

- ✅ Проверять, что все используемые пакеты установлены
- ✅ Убеждаться, что версии зависимостей совместимы

## Алгоритм работы:

1. **После каждого изменения файла:**
   - Вызвать `read_lints` для этого файла
   - Исправить все найденные ошибки
   - Проверить связанные файлы

2. **Перед завершением задачи:**
   - Проверить все измененные файлы через `read_lints`
   - Запустить `npm run lint` для проверки кода через ESLint
   - Запустить `npm run format:check` для проверки форматирования
   - Запустить `npm run type-check` для проверки типов TypeScript
   - Убедиться, что нет ошибок типизации
   - Проверить, что проект компилируется без ошибок

3. **При работе с импортами:**
   - Проверять корректность путей импорта
   - Убеждаться, что типы для импортируемых модулей определены
   - Для Vue компонентов проверять наличие деклараций типов

## Примеры проблем, которые нужно проверять:

- ❌ Ошибки типа: `Cannot find module '@/components/Home.vue' or its corresponding type declarations`
- ❌ Неиспользуемые переменные и импорты
- ❌ Ошибки типизации TypeScript
- ❌ Неразрешенные импорты
- ❌ Отсутствующие зависимости

## Правило:

**НИКОГДА не завершать задачу, если есть ошибки линтера или типизации!**

**ОБЯЗАТЕЛЬНО:** После каждой задачи запускать `npm run type-check` для проверки типов TypeScript.
